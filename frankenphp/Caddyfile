{
    order php_server before file_server

    frankenphp {

    }

    log {
            output stdout
            level DEBUG
        }
}

{$SERVER_NAME:localhost} {
    root * /app/public

    encode zstd br gzip

    header ?Permissions-Policy "browsing-topics=()"

    php_server
}