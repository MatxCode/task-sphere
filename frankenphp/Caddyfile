{
    auto_https off
    order php before file_server
}

:{$PORT} {
    root * /app/public

    # Ancienne syntaxe compatible
    php_frankenphp {
        resolve_root_symlink
        split .php
    }

    file_server
}